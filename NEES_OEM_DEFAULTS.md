# Nees OEM Defaults v1

Default state is enabled and global.

Speaker: limiter + moderate gain control + punch EQ + psychoacoustic bass + ViPER bass + clarity + restrained stereo imaging.
Headphones: separate deeper-bass profile with restrained widening.

Boot persistence: BOOT_COMPLETED, MY_PACKAGE_REPLACED, USER_UNLOCKED fallback.
One-time marker: nees_oem_defaults_v1, so later user tuning is not reset every service start.
